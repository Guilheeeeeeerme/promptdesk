# Guardrails

<span class="status status-verified">VERIFIED</span> PromptDesk `docs/guardrails.md` é a **referência**. Argus e Quizzeira devem alinhar os mesmos contratos.

Resumo: registry versionado → screening determinístico → contexto não confiável só em bloco cercado → LLM só texto → parse estrito / fail closed → sem log de corpos de mensagem.

Detalhe completo: [English](/en/standards/guardrails).
