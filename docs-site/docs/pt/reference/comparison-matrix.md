# Matriz comparativa

Resumo: PromptDesk = BullMQ + dual Postgres; Quizzeira = runLoop + 3 planos; Argus = pipeline de visão + prompt-eval. Sem Agents / sem streaming de tokens nos três.

Tabela completa: [English](/en/reference/comparison-matrix).
